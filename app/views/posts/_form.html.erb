<%= form_for @post do |f| %>

	<div class="form-group">
		<%= f.label 'message' %>
		<%= f.text_area :body, class: 'form-control' %>
	</div>

	<div class="form-group">
		<%= f.submit 'submit', :class => 'btn btn-success' %>
	</div>


<% end %>
